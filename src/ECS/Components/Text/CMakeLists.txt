add_library(arcade_text SHARED TextComponent.cpp)

set_target_properties(arcade_text PROPERTIES PREFIX "" OUTPUT_NAME "arcade_text")
target_include_directories(arcade_text PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/../../.. ${CMAKE_CURRENT_SOURCE_DIR}/.. ${CMAKE_ROOT}/src/Shared)
if(ENABLE_COVERAGE)
    target_compile_options(arcade_text PRIVATE -O0 -g --coverage -fprofile-arcs -ftest-coverage)
    target_link_libraries(arcade_text PRIVATE --coverage)
else ()
    target_link_libraries(arcade_text PRIVATE)
endif()
